const DATA_PATHS=['data/resources.json','data/additional-resources.json'];
const esc=(s='')=>s.replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c]));
function card(r){return `<article class="resource-card"><div class="resource-meta"><span class="badge">${esc(r.type)}</span><span class="tag">${esc((r.languages||[]).join(' · '))}</span></div><h3>${esc(r.name)}</h3><p>${esc(r.description)}</p><div class="tags">${(r.categories||[]).slice(0,3).map(x=>`<span class="tag">${esc(x)}</span>`).join('')}</div><a class="resource-link" href="resource.html?id=${encodeURIComponent(r.id)}">Details →</a></article>`}
async function getResources(){const sets=await Promise.all(DATA_PATHS.map(p=>fetch(p).then(r=>r.ok?r.json():[])));const seen=new Set();return sets.flat().filter(r=>{if(seen.has(r.id))return false;seen.add(r.id);return true})}
async function load(){try{const resources=await getResources();const featured=document.querySelector('#featuredGrid');if(featured)featured.innerHTML=resources.filter(r=>r.featured).slice(0,8).map(card).join('');const input=document.querySelector('#searchInput');const clear=document.querySelector('#clearSearch');if(input){const go=q=>{const value=q.trim();if(value)window.location.href=`resources.html?search=${encodeURIComponent(value)}`};input.addEventListener('keydown',e=>{if(e.key==='Enter')go(input.value)});clear?.addEventListener('click',()=>input.value='');document.querySelectorAll('[data-query]').forEach(b=>b.addEventListener('click',()=>{input.value=b.dataset.query;go(input.value)}));}}catch(e){console.error('Could not load resources',e)}}
document.addEventListener('DOMContentLoaded',load);