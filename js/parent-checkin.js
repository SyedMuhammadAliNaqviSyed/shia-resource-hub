const messages={worked:['MashaAllah. Keep the next step small and consistent.'], 'needed-help':['That is useful feedback. The next step can include discussion with a parent, teacher or alim rather than simply adding more content.'], 'too-long':['Make it smaller. A shorter activity that is actually completed is better than an overwhelming plan.'], 'not-interested':['Try changing the format or topic before increasing the amount of content. A video, story, audio or activity may fit better.']};
const key='shia-resource-hub-parent-checkins';
document.addEventListener('DOMContentLoaded',()=>{const out=document.getElementById('checkinResult');document.querySelectorAll('.checkin-card').forEach(card=>card.addEventListener('click',()=>{const v=card.dataset.value;let data={};try{data=JSON.parse(localStorage.getItem(key)||'{}')}catch{}data[v]=(data[v]||0)+1;localStorage.setItem(key,JSON.stringify(data));out.hidden=false;out.innerHTML=`<p class="eyebrow">Next step</p><h2>${messages[v][0]}</h2><p>This check-in is guidance for your next decision; it is not a judgment about your child.</p>`;document.querySelectorAll('.checkin-card').forEach(x=>x.classList.remove('is-selected'));card.classList.add('is-selected')})});