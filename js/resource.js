const esc=(s='')=>s.replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c]));
document.addEventListener('DOMContentLoaded',async()=>{const box=document.querySelector('#resourceDetail');try{const resources=await fetch('data/resources.json').then(r=>r.json());const id=new URLSearchParams(location.search).get('id');const r=resources.find(x=>x.id===id);if(!r){box.innerHTML='<div class="empty"><h2>Resource not found</h2><p>It may have been moved or removed from the directory.</p><a class="button" href="resources.html">Browse resources</a></div>';return}document.title=`${r.name} — Shia Resource Hub`;box.innerHTML=`<p class="eyebrow">${esc(r.type)}</p><h1>${esc(r.name)}</h1><p class="detail-description">${esc(r.description)}</p><div class="detail-grid"><div><h3>Categories</h3><div class="tags">${r.categories.map(x=>`<span class="tag">${esc(x)}</span>`).join('')}</div></div><div><h3>Languages</h3><div class="tags">${r.languages.map(x=>`<span class="tag">${esc(x)}</span>`).join('')}</div></div><div><h3>Useful for</h3><div class="tags">${(r.audience||[]).map(x=>`<span class="tag">${esc(x)}</span>`).join('')}</div></div><div><h3>Tags</h3><div class="tags">${(r.tags||[]).map(x=>`<span class="tag">${esc(x)}</span>`).join('')}</div></div></div><div class="detail-actions"><a class="button" href="${esc(r.url)}" target="_blank" rel="noopener noreferrer">Visit resource →</a><a class="button secondary-button" href="resources.html">Explore more resources</a></div>`}catch(e){box.innerHTML='<div class="empty">Unable to load this resource right now.</div>'}});