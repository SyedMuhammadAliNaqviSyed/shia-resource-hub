const esc=(s='')=>s.replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c]));
const card=r=>`<article class="resource-card"><div class="resource-meta"><span class="badge">${esc(r.type)}</span><span class="tag">${esc((r.languages||[]).join(' · '))}</span></div><h3>${esc(r.name)}</h3><p>${esc(r.description)}</p><div class="tags">${(r.categories||[]).map(x=>`<span class="tag">${esc(x)}</span>`).join('')}${(r.audience||[]).slice(0,2).map(x=>`<span class="tag">${esc(x)}</span>`).join('')}</div><div class="card-actions"><a class="resource-link" href="resource.html?id=${encodeURIComponent(r.id)}">Details →</a><a class="resource-link secondary" href="${esc(r.url)}" target="_blank" rel="noopener noreferrer">Visit →</a></div></article>`;
document.addEventListener('DOMContentLoaded',async()=>{const resources=await fetch('data/resources.json').then(r=>r.json());const grid=document.querySelector('#resourceGrid'),filters=document.querySelector('#filters'),input=document.querySelector('#directorySearch'),count=document.querySelector('#resultCount'),language=document.querySelector('#languageFilter'),type=document.querySelector('#typeFilter');if(!grid||!filters||!input)return;const cats=['All',...new Set(resources.flatMap(r=>r.categories||[]))];let active=new URLSearchParams(location.search).get('category')||'All';input.value=new URLSearchParams(location.search).get('search')||'';function drawFilters(){filters.innerHTML=cats.map(c=>`<button class="filter-btn ${c===active?'active':''}" data-cat="${esc(c)}">${esc(c)}</button>`).join('');filters.querySelectorAll('button').forEach(b=>b.onclick=()=>{active=b.dataset.cat;drawFilters();render()})}function render(){const q=input.value.trim().toLowerCase(),lang=language?.value||'All',fmt=type?.value||'All';const shown=resources.filter(r=>(active==='All'||(r.categories||[]).includes(active))&&(lang==='All'||(r.languages||[]).includes(lang))&&(fmt==='All'||r.type===fmt)&&(!q||[r.name,r.description,r.type,...(r.categories||[]),...(r.languages||[]),...(r.tags||[]),...(r.audience||[])].join(' ').toLowerCase().includes(q)));count.textContent=`${shown.length} resource${shown.length===1?'':'s'} found`;grid.innerHTML=shown.length?shown.map(card).join(''):'<div class="empty">No resources matched your filters. Try another keyword or reset the filters.</div>'}input.oninput=render;if(language)language.onchange=render;if(type)type.onchange=render;const clear=document.querySelector('#directoryClear');if(clear)clear.onclick=()=>{input.value='';render()};const reset=document.querySelector('#resetFilters');if(reset)reset.onclick=()=>{active='All';input.value='';if(language)language.value='All';if(type)type.value='All';drawFilters();render()};drawFilters();render()});