const esc=(s='')=>s.replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c]));
const DATA_PATHS=['data/resources.json','data/additional-resources.json','data/research-resources.json','data/urdu-south-asia-resources.json','data/expansion-batch-01.json'];
const quality=r=>{const b=[];if(r.verified)b.push('<span class="quality-badge verified">✓ Verified</span>');if(r.official)b.push('<span class="quality-badge official">Official</span>');if(r.free)b.push('<span class="quality-badge free">Free</span>');return b.join('')};
const card=r=>`<article class="resource-card"><div class="resource-meta"><span class="badge">${esc(r.type)}</span><span class="tag">${esc((r.languages||[]).join(' · '))}</span></div>${quality(r)?`<div class="quality-row">${quality(r)}</div>`:''}<h3>${esc(r.name)}</h3><p>${esc(r.description)}</p><div class="tags">${(r.categories||[]).map(x=>`<span class="tag">${esc(x)}</span>`).join('')}${(r.audience||[]).slice(0,2).map(x=>`<span class="tag">${esc(x)}</span>`).join('')}</div><div class="card-actions"><a class="resource-link" href="resource.html?id=${encodeURIComponent(r.id)}">Details →</a><a class="resource-link secondary" href="${esc(r.url)}" target="_blank" rel="noopener noreferrer">Visit →</a></div></article>`;
async function getResources(){const [sets,qualityData]=await Promise.all([Promise.all(DATA_PATHS.map(p=>fetch(p).then(r=>r.ok?r.json():[]))),fetch('data/quality.json').then(r=>r.ok?r.json():[])]);const qualityMap=new Map(qualityData.map(x=>[x.id,x]));const seen=new Set();return sets.flat().filter(r=>{if(!r||!r.id||seen.has(r.id))return false;seen.add(r.id);return true}).map(r=>({...r,...(qualityMap.get(r.id)||{})}))}
document.addEventListener('DOMContentLoaded',async()=>{const resources=await getResources();const grid=document.querySelector('#resourceGrid'),filters=document.querySelector('#filters'),input=document.querySelector('#directorySearch'),count=document.querySelector('#resultCount'),language=document.querySelector('#languageFilter'),type=document.querySelector('#typeFilter'),qualityFilter=document.querySelector('#qualityFilter'),sort=document.querySelector('#sortFilter');if(!grid||!filters||!input)return;const cats=['All',...new Set(resources.flatMap(r=>r.categories||[]))];let active=new URLSearchParams(location.search).get('category')||'All';input.value=new URLSearchParams(location.search).get('search')||'';function drawFilters(){filters.innerHTML=cats.map(c=>`<button class="filter-btn ${c===active?'active':''}" data-cat="${esc(c)}">${esc(c)}</button>`).join('');filters.querySelectorAll('button').forEach(b=>b.onclick=()=>{active=b.dataset.cat;drawFilters();render()})}function render(){const q=input.value.trim().toLowerCase(),lang=language?.value||'All',fmt=type?.value||'All',quality=qualityFilter?.value||'All',order=sort?.value||'relevance';let shown=resources.filter(r=>(active==='All'||(r.categories||[]).includes(active))&&(lang==='All'||(r.languages||[]).includes(lang))&&(fmt==='All'||r.type===fmt)&&(quality==='All'||Boolean(r[quality]))&&(!q||[r.name,r.description,r.type,...(r.categories||[]),...(r.languages||[]),...(r.tags||[]),...(r.audience||[])].join(' ').toLowerCase().includes(q)));if(order==='name')shown.sort((a,b)=>a.name.localeCompare(b.name));else if(order==='verified')shown.sort((a,b)=>Number(b.verified)-Number(a.verified)||a.name.localeCompare(b.name));else if(order==='free')shown.sort((a,b)=>Number(b.free)-Number(a.free)||a.name.localeCompare(b.name));else if(order==='featured')shown.sort((a,b)=>Number(b.featured)-Number(a.featured)||Number(b.verified)-Number(a.verified));else if(order==='newest')shown.sort((a,b)=>new Date(b.addedAt||b.updatedAt||0)-new Date(a.addedAt||a.updatedAt||0));count.textContent=`${shown.length} resource${shown.length===1?'':'s'} found`;grid.innerHTML=shown.length?shown.map(card).join(''):'<div class="empty">No resources matched your filters. Try another keyword or reset the filters.</div>'}input.oninput=render;if(language)language.onchange=render;if(type)type.onchange=render;if(qualityFilter)qualityFilter.onchange=render;if(sort)sort.onchange=render;const clear=document.querySelector('#directoryClear');if(clear)clear.onclick=()=>{input.value='';render()};const reset=document.querySelector('#resetFilters');if(reset)reset.onclick=()=>{active='All';input.value='';if(language)language.value='All';if(type)type.value='All';if(qualityFilter)qualityFilter.value='All';if(sort)sort.value='relevance';drawFilters();render()};drawFilters();render()});