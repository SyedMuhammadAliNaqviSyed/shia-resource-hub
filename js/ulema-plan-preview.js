const escP=s=>String(s||'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c]));
const q=new URLSearchParams(location.search);let plans=[];try{plans=JSON.parse(decodeURIComponent(escape(atob(q.get('plans')||''))))}catch{}
const preview=document.getElementById('preview');
if(!plans.length){preview.innerHTML='<div class="empty"><h2>No learning plan found.</h2><p>Open a plan from Ulema support first.</p><a class="button" href="ulema-support.html">Create a plan →</a></div>'}else{const age=q.get('age')||'Learner',goal=q.get('goal')||'Learning';preview.innerHTML=`<div class="support-result-head"><div><p class="eyebrow">Plan preview</p><h2>${escP(goal)}</h2><p>For age ${escP(age)} · ${plans.length} resources</p></div><span class="badge">Ready to review</span></div><div class="plan-list">${plans.sort((a,b)=>(+a.order||0)-(+b.order||0)).map((p,i)=>`<article class="plan-item"><div class="plan-number">${i+1}</div><div class="plan-main"><h3>${escP(p.title||p.name||'Selected resource')}</h3><p>${escP(p.instruction||'No specific instruction added yet.')}</p><div class="plan-fields"><span><strong>${escP(p.minutes||10)}</strong> min</span><span><strong>${escP(p.frequency||'once')}</strong></span></div></div></article>`).join('')}</div><div class="task-box"><strong>Final check</strong><p>Is every step useful, understandable for this learner, and realistic with the family's available time?</p></div><div class="support-actions"><a class="button" href="${location.href.replace('ulema-plan-preview.html','parent-plan.html')}">Open parent view →</a><a class="button secondary" href="${location.href.replace('ulema-plan-preview.html','learner.html')}">Open learner view →</a></div>`}